# 历史实现归档

这些文件保留用于查阅，不是 1.0.3 的应用入口，也不参与打包。

- `browser-settings/`：旧浏览器设置页和 C# 浏览器启动器源码。
- `sync-to-d.ps1`：旧机器路径的复制脚本，路径已经过时；不要直接运行。

当前入口：项目主目录 `Live2D-Desktop-Assistant.exe`；开发入口 `main.js`；控制面板 `src/settings.html`。
